{
  "name": "bmi-event",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npx supabase start\"",
    "frontend": "npm run dev --prefix frontend",
    "supabase": "npx supabase start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
